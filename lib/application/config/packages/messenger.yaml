framework:
    messenger:
        buses:
            command.bus:
                middleware:
                    - validation

            query.bus:
                middleware:
                    - validation
